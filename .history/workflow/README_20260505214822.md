# Workflow

- Purpose: describe team process from request to delivery.
- Use compact style for all docs outside `/specs`.
- Start with `../init/README.md` if new member.
- Follow `workflow/process.md` for request handling.
- User requests start in `user_requests/`.
- Stable user-facing specs go to `user_specs/`.
- Use `decisions/` for formal decisions.
- Use `archive/` for closed threads/items.
