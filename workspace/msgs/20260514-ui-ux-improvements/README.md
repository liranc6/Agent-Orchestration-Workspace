# SITREP: 20260514-ui-ux-improvements

ORCH > ACK @ user_req | +FAB | +multi-page | ^ UX.
ORCH > CONSULT: XAA | XPM.

XAA > p/w: req +FAB +modal +multi-page. 
XAA > ? target pages: Dashboard, History, Stats. 
XAA > ? tech stack: Streamlit? (detected in environment_info). Multi-page in Streamlit requires `pages/` dir or routing logic.
XAA > ! BLOCK: multi-page vs "one-pager" hx. 

XPM > goal: +accessibility | +organization.
XPM > +FAB @ bottom-right | ^ add_expense flow.
XPM > +multi-page => [Summary, Ledger, Analytics].
XPM > CONSULT: XAD.
